#!/usr/bin/env node
'use strict';
const { main } = require('../control-plane/marketing-workflows/production-entrypoint');
try { main(); } catch (error) { process.stderr.write(JSON.stringify({ status: 'error', error: error.message }) + '\n'); process.exitCode = 1; }
